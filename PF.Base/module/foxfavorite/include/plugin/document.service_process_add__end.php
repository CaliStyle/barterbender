<?phpif ($aInsert['privacy'] == 0 && $aInsert['is_approved'] == 1){    $aUsers = phpfox::getService('foxfavorite')->getUserInfoToSendNotification();    foreach ($aUsers as $iKey => $aUser)    {        if (isset($aUser['user_notification']) && ($aUser['user_notification']))        {                    }        else        {            Phpfox::getService('notification.process')->add('foxfavorite_adddocument', $iDocumentId, $aUser['user_id']);        }    }}?>